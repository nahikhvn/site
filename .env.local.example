# Sync script only (server-side, never sent to the browser)
HARDCOVER_TOKEN=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Portfolio (read-only, exposed to the browser)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
